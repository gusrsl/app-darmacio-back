const { handleErrors } = require('./handleErrors')
const notFound = require('./notFound')
const securityPolicy = require('./securityPolicy')

module.exports = { handleErrors, notFound, securityPolicy }
